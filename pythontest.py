foo = [5, 4, 3, 2, 1]  # Add spaces after commas per PEP 8
foo.sort()            # sort() modifies the list in-place and returns None
min_val = foo[0]      # After sorting, the minimum value is the first element
print("min of foo array is ", str(min))
